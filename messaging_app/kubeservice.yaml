apiVersion: v1
kind: Service
metadata:
  name: django-messaging-service
spec:
  type: ClusterIP
  selector:
    app: django-messaging
    version: blue # Start with blue, later change to green
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
